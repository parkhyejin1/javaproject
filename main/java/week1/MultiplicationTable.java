package week1;

public class MultiplicationTable {
    //구구단
    public static void main(String[] args) {
        // 2 * 1 = 2
        // 2 * 2 = 4
        System.out.println("2" + "*" + "1" + "=" + "2");
        System.out.printf("%d * %d = %d", 2, 2, 4);
       // %d로 식의 연산 결과 출력
        System.out.println("2" + "*" + "1" + "=" + "2");
        System.out.printf("%d * %d = %d\n", 2, 2, 4);
        System.out.printf("%d * %d = %d", 2, 3, 2 * 3);
        //결과
        //2*1=2
        //2 * 2 = 4
        //2 * 3 = 6
//에러 메세지에서 Double / Float이 다른 것에 주목하기(0.5는 기본실수라서 double / 0.5f는 float) float은 소수점 6번째 자리까지 정확하게 표현할 수 있습니다.
//Java의 기본 실수 표현은 Double,  Float는 f(F)를 붙여 표현
//Float → floating
//Double → floating double
       // System.out.printf("%f", 1 / 2);
       // f != java.lang.Integer

       // Integer → 정수
               // -1, 0, 1, 2, 3, 4








    }

}
